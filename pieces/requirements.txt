# Standalone mesh-generation stack for the piece generator.
# The CORE pipeline (silhouette -> 2D quality mesh -> height field -> closed
# manifold -> invariant validation) runs with these alone — NO Blender needed.
# Blender 5.1.x stays optional, only for rendering / sculpting / connector graft.
#
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r pieces/requirements.txt

# --- core numerics (already present system-wide here: numpy 1.21, scipy 1.8) ---
numpy>=1.21
scipy>=1.8

# --- silhouette parsing: SVG path (beziers/arcs) -> polyline ---
svgelements>=1.9

# --- 2D quality meshing: Delaunay refinement w/ guaranteed minimum angle ---
# (Shewchuk's Triangle, via Rufat's bindings.)
triangle>=20230923

# --- 2D polygon ops: area, point-in-polygon, inward offset/erosion ---
shapely>=2.0

# --- mesh I/O (OBJ/STL/GLB) + ray casting for the silhouette/draft invariants ---
# (replaces Blender's BVHTree, so invariants.py can run in plain pytest) ---
trimesh>=4.0
networkx>=2.6           # trimesh graph ops: winding repair, face adjacency

# --- diagnostics / explanatory diagrams ---
matplotlib>=3.5

# --- optional: raster medial-axis / skeleton, only if we need an explicit spine ---
# scikit-image>=0.21
